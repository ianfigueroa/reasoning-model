# Install CUDA torch FIRST from the cu126 index (has cp313/Python-3.13 wheels), then:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
torch>=2.6              # cp313 CUDA wheels start at cu124 (2.6) / cu126 (2.9)
transformers>=4.44
datasets>=2.20
accelerate>=0.33
peft>=0.12
trl>=0.9
bitsandbytes>=0.45     # 4-bit QLoRA; 0.50+ ships an official Windows wheel
evaluate>=0.4
matplotlib>=3.8
